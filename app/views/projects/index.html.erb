<% reset_cycle %>
<table class="list">
  <thead>
    <tr>
      <th>ID</th>
      <th>Name</th>
      <th>Description</th>
      <th>Created on</th>
      <th>Updated on</th>
      <th></th>
      <th></th>
    </tr>
  </thead>
  <tbody>
  <% @projects.each do |p| %>
    <tr class="<%= cycle("odd", "even") %>">
      <td><%= p.id %></td>
      <td><%= p.name %></td>
      <td><%= p.description %></td>
      <td><%= p.created_on.strftime("%F") %></td>
      <td><%= p.updated_on.strftime("%F") %></td>
      <td><%= link_to( image_tag("edit.png",      :size => "16x16"), "/projects/edit?id=#{p.id}",  :title=>"Edit" ) %></td>
      <td><%= link_to( image_tag("dashboard.png", :size => "16x16"), "/dashboard?project=#{p.id}", :title=>"Dashboard" ) %></td>
    </tr>
  <% end %>
  </tbody>
<table>
